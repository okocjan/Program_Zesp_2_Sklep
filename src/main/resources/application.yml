spring:
  application:
    name: Nazwa do ustalenia
  datasource:
    url: jdbc:postgresql://localhost:49153/postgres
    username: postgres
    password: postgrespw
  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
    properties:
      hibernate:
        format_sql: true
        dialect: org.hibernate.dialect.PostgreSQL81Dialect
  flyway:
    url: jdbc:postgresql://localhost:49153/postgres
    schemas: storedb
    user: postgres
    password: postgrespw